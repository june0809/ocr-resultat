{
  "name": "ocr-resultat",
  "version": "0.2.0",
  "private": true,
  "description": "Ingestion de resultats de match par capture d'ecran : service de validation + moteur OCR reutilisable (navigateur et Node).",
  "scripts": {
    "predev": "node scripts/sync-tesseract-assets.mjs",
    "dev": "next dev",
    "prebuild": "node scripts/sync-tesseract-assets.mjs",
    "build": "next build",
    "assets": "node scripts/sync-tesseract-assets.mjs",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "keygen": "node scripts/keygen.mjs",
    "keygen:scan": "node scripts/keygen-scan.mjs",
    "e2e": "tsx scripts/ocr-e2e.ts",
    "build:engine": "tsc -p tsconfig.build.json",
    "check:dist": "node scripts/check-dist.mjs",
    "check:package": "node scripts/check-package.mjs",
    "check:secrets": "node scripts/check-secrets.mjs",
    "prepare": "tsc -p tsconfig.build.json"
  },
  "//exports": [
    "Le moteur est expose comme BIBLIOTHEQUE : une application tierce (The Circle)",
    "le declare en dependance git et importe 'ocr-resultat/browser'. Le code reste",
    "ici — une seule source de verite, pas de recopie a maintenir des deux cotes.",
    "",
    "dist/ est COMMITE (cf. .gitignore) : le paquet ne peut donc jamais s'installer",
    "vide. 'prepare' reste la pour le confort de dev (build automatique apres un",
    "npm install ici), mais la livraison n'en DEPEND plus — une install sans scripts",
    "posait auparavant un paquet sans dist/, en sortant en succes.",
    "",
    "Verifie par 'npm run check:package' (pack + install --ignore-scripts + import",
    "des exports) et 'npm run check:dist' (dist/ en phase avec lib/ocr/).",
    "",
    "./pseudo est expose seul volontairement : c'est une utilite PURE, sans",
    "dependance a tesseract.js (lequel ne rend pas la main hors navigateur). On",
    "peut donc la tester ou la reutiliser sans charger le moteur entier.",
    "",
    "ATTENTION : le champ 'exports' n'accepte que des chemins commencant par '.'",
    "— y glisser une cle de commentaire casse la resolution de Node."
  ],
  "files": [
    "dist"
  ],
  "exports": {
    "./browser": {
      "types": "./dist/browser.d.ts",
      "default": "./dist/browser.js"
    },
    "./server": {
      "types": "./dist/server.d.ts",
      "default": "./dist/server.js"
    },
    "./template": {
      "types": "./dist/template.d.ts",
      "default": "./dist/template.js"
    },
    "./pseudo": {
      "types": "./dist/pseudo.d.ts",
      "default": "./dist/pseudo.js"
    },
    "./package.json": "./package.json"
  },
  "//dependencies": [
    "Ne garder ici QUE ce dont le moteur a besoin pour tourner. next/react/zod sont",
    "passes en devDependencies : npm n'installe JAMAIS les devDependencies d'une",
    "dependance, donc une application qui consomme ce paquet ne tire ni Next.js ni",
    "React. C'est ce qui rend l'usage en bibliotheque viable sans repo separe.",
    "",
    "@tesseract.js-data/eng reste une vraie dependance : le consommateur y prend la",
    "traineddata a servir depuis son propre domaine, plutot que de committer un",
    "binaire de 15 Mo dans son repo ou de dependre d'un CDN.",
    "",
    "sharp n'est utile qu'a l'export ./server. Le consommateur type (The Circle)",
    "n'importe que ./browser, qui passe par canvas : lui imposer un binaire natif",
    "de plusieurs dizaines de Mo n'aurait aucun sens. D'ou la peer dependency",
    "OPTIONNELLE — et non une optionalDependency, que npm installe malgre tout.",
    "Une peer marquee optionnelle dans peerDependenciesMeta n'est jamais installee",
    "d'office : qui veut ./server declare sharp de son cote."
  ],
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "tesseract.js": "^5.1.1"
  },
  "peerDependencies": {
    "sharp": "^0.35.3"
  },
  "peerDependenciesMeta": {
    "sharp": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "next": "^16.2.10",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "sharp": "^0.35.3",
    "tsx": "^4.23.1",
    "typescript": "5.5.3",
    "zod": "3.23.8"
  },
  "overrides": {
    "postcss": ">=8.5.10"
  }
}
